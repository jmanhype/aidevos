# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# Where 3rd-party dependencies like ExDoc output generated docs.
/doc/

# Ignore .fetch files in case you like to edit your project deps locally.
/.fetch

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Ignore package tarball (built via "mix hex.build").
realworld-*.tar

# Ignore assets that are produced by build tools.
/priv/static/assets/

# Ignore digested assets cache.
/priv/static/cache_manifest.json

# In case you use Node.js/npm, you want to ignore these.
npm-debug.log
/assets/node_modules/

# Environment files containing sensitive information
.env
.env.*
!.env.example
<<<<<<< HEAD
=======

# Test files containing API keys
test_multi_agent_development.exs
debug_huggingface_api.exs
debug_proxy_lite_connection.exs
test_huggingface_connection.exs

# Script folders that might contain sensitive information
/scripts/test_callback.exs
/scripts/test_vsm_callback.exs
/scripts/update_twilio_config.exs
>>>>>>> 466e18a (Security: Update configuration files to use environment variables instead of hardcoded secrets and improve .gitignore to exclude sensitive files)

# Environment files containing sensitive information
.env
.env.*
!.env.example

# Test files containing API keys
test_multi_agent_development.exs
debug_huggingface_api.exs
debug_proxy_lite_connection.exs
test_huggingface_connection.exs

# Script folders that might contain sensitive information
/scripts/test_callback.exs
/scripts/test_vsm_callback.exs
/scripts/update_twilio_config.exs
